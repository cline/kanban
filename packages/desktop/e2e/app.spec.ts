/**
 * Placeholder spec — proves the Playwright harness runs.
 * Replace with real Electron E2E tests once the launch helper is wired up.
 */
import { test, expect } from "@playwright/test";

test.describe("desktop e2e harness", () => {
	test("placeholder – harness boots", () => {
		expect(true).toBe(true);
	});
});
