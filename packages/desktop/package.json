{
	"name": "@kanban/desktop",
	"version": "0.0.1",
	"private": true,
	"description": "Kanban desktop app — Electron shell for the Kanban runtime",
	"type": "module",
	"main": "dist/main.js",
	"scripts": {
		"clean": "shx rm -rf dist out cli",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"stage:cli": "node scripts/stage-cli.mjs",
		"build:ts": "tsc -p tsconfig.build.json && esbuild src/preload.ts --bundle --platform=node --format=cjs --external:electron --outfile=dist/preload.js && shx cp src/disconnected.html dist/disconnected.html",
		"build": "npm run stage:cli && npm run build:ts && electron-builder --config electron-builder.yml",
		"build:mac:dmg": "npm run stage:cli && npm run build:ts && electron-builder --mac dmg --config electron-builder.yml",
		"build:mac:arm64": "npm run stage:cli && npm run build:ts && electron-builder --mac dmg --arm64 --config electron-builder.yml",
		"build:mac:x64": "npm run stage:cli && npm run build:ts && electron-builder --mac dmg --x64 --config electron-builder.yml",
		"dev": "tsc -p tsconfig.build.json && node scripts/launch-electron.mjs",
		"start": "electron .",
		"postinstall": "electron-builder install-app-deps && node scripts/patch-node-pty.mjs",
		"test": "vitest run --passWithNoTests",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"@electron/notarize": "^3.0.0",
		"@types/node": "^22.10.5",
		"electron": "^41.10.3",
		"electron-builder": "^26.8.1",
		"esbuild": "^0.28.1",
		"shx": "^0.4.0",
		"typescript": "^5.9.3",
		"vitest": "^4.1.0"
	},
	"dependencies": {
		"node-pty": "^1.2.0-beta.11"
	},
	"overrides": {
		"undici": ">=6.27.0 <7",
		"esbuild": "$esbuild",
		"js-yaml": ">=4.3.1 <5",
		"nanoid@<4": ">=3.3.17 <4",
		"postcss": ">=8.5.23 <9"
	}
}
